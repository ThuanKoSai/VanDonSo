# RPC endpoint Sepolia (Infura/Alchemy)
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY

# Private key ví dùng để deploy (KHÔNG commit file .env thật lên Git!)
PRIVATE_KEY=

# API key để verify contract trên Etherscan
ETHERSCAN_API_KEY=
